[user]
	name = Chester Lam
	email = chesterlam620@gmail.com
	signingkey = C8DC390BE7957137

[alias]
  co = checkout
  ci = commit -v
  cia = commit --amend
  st = status
  br = branch
  rb = rebase
  d = diff
  a = add
  f = fetch
  cl = clone
  hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
  type = cat-file -t
  dump = cat-file -p
  graph = log --graph --pretty=format:\"%C(yellow)%h%Cblue%d%C(reset) %s %C(white) %an, %ar%C(reset)\"
  lol = log --oneline
  sta = stash
  stap = stash pop
  stal = stash list

[core]
	editor = vim
	excludesfile = /Users/chesterlam/.global_ignore

[push]
        ui = auto
[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
[commit]
	gpgsign = true
